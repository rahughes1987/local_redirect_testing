{
  "name": "redirect-tests",
  "version": "1.0.0",
  "description": "test the httpd.config redirects",
  "main": "redirect.tests.js",
  "dependencies": {
    "axios": "^0.19.2"
  },
  "devDependencies": {
    "mocha": "^10.2.0"
  },
  "scripts": {
    "test": "docker build . --no-cache"
  }
}
